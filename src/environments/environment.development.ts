// src/environments/environment.developement.ts
// used when we run `ng serve --environment developement` or `ng build --environment developement`
export const environment = {
    production: false,
  
    // URL of development API
    apiUrl: 'http://localhost:3000'
  };